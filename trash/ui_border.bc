#include "ui_border.h"

const UIBorder UI_BORDER_NONE = { .enabled = false };
const UIBorder UI_BORDER_SINGLE = {
    .enabled = true,
    .ls = '|', .rs = '|',
    .ts = '-', .bs = '-',
    .tl = '+', .tr = '+',
    .bl = '+', .br = '+'
};